/**
 * Google sign-in is initiated via the API (GET /auth/google/start).
 * OAuth client credentials and redirect URI configuration are not bundled
 * in the app. Redirect URIs are owned by the backend/browser auth flow.
 */
