# Fucci Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-10-23

## Active Technologies
- TypeScript (React Native/Expo), Go 1.22+ (backend) + React Navigation, existing auth API (JWT), 001 user/follow APIs (005-user-registration-settings)
- PostgreSQL (users, user_follows); existing schema in 001; may add first_name, last_name, avatar_url if not present (005-user-registration-settings)
- TypeScript (React Native/Expo), Go 1.22+ (backend) + React Navigation, existing debate API (004), auth API (005), JWT (006-user-engagement-debates)
- PostgreSQL; extend `comments` (add `seeded`); new tables `comment_votes`, `comment_reactions` (006-user-engagement-debates)
- Go 1.22+, TypeScript (React Native / Expo) + chi (HTTP), React Navigation, expo; JWT auth (005) (006-user-engagement-debates)
- PostgreSQL (debates, debate_cards, comments, votes, comment_votes, comment_reactions); Redis for cache (006-user-engagement-debates)
- React Native (TypeScript) + Expo; Go 1.22 API; PostgreSQL 15 + React Navigation, React Query (if used), React Native Image Picker, existing Go HTTP stack, S3-compatible object storage (007-player-profile)
- PostgreSQL tables for player profiles, traits, and career teams; S3 (or equivalent) for profile photos (007-player-profile)
- Go 1.22+ (API), TypeScript strict (Expo / React Native mobile) + chi HTTP router (existing), Cloudinary signing (Go: HMAC/SHA1 per Cloudinary docs or official SDK), Expo ImagePicker + `fetch`/`FormData` for upload on mobile (008-photo-upload-cloudinary)
- PostgreSQL existing columns `users.avatar_url`, `player_profile.photo_url`; binary blobs not stored in DB (008-photo-upload-cloudinary)
- Go 1.22+ (API), TypeScript strict (Expo / React Native 0.81+) + chi router, sqlc-generated queries, React Navigation 7, TanStack React Query (existing) (009-debate-tab-navigator)
- PostgreSQL (`debates`, `debate_cards`, `votes`, `debate_analytics`, …) (009-debate-tab-navigator)
- Go 1.24 toolchain (`services/api/go.mod`) + Go stdlib (`net/http`, `encoding/json`), internal cache interface (`services/api/internal/cache`), existing HTTP utility (`services/api/internal/api/utils.go`) (019-modularize-futbol-package)
- N/A for this refactor (no schema/data model change required) (019-modularize-futbol-package)
- Go 1.24 toolchain (`services/api/go.mod`) + Go stdlib, existing internal cache interface (`services/api/internal/cache`), existing API HTTP helpers in `services/api/internal/api` (019-modularize-futbol-package)
- N/A (no schema/storage changes in this feature) (019-modularize-futbol-package)

- TypeScript (React Native), Go 1.22+, SQL + React Native with Expo, Gin framework, PostgreSQL, Redis, OpenAI API, Socket.io (001-football-community)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript (React Native), Go 1.22+, SQL: Follow standard conventions

## Recent Changes
- 019-modularize-futbol-package: Added Go 1.24 toolchain (`services/api/go.mod`) + Go stdlib, existing internal cache interface (`services/api/internal/cache`), existing API HTTP helpers in `services/api/internal/api`
- 019-modularize-futbol-package: Added Go 1.24 toolchain (`services/api/go.mod`) + Go stdlib (`net/http`, `encoding/json`), internal cache interface (`services/api/internal/cache`), existing HTTP utility (`services/api/internal/api/utils.go`)
- 009-debate-tab-navigator: Added Go 1.22+ (API), TypeScript strict (Expo / React Native 0.81+) + chi router, sqlc-generated queries, React Navigation 7, TanStack React Query (existing)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
